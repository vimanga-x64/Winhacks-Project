/* ========================================================
   FitTrack Dashboard – matches Landing Page aesthetic
   ======================================================== */

/* ---------- ROOT ---------- */
.fa-root {
  position: relative;
  min-height: 100vh;
  background: #0a0a0a;
  color: #f0f0f0;
  font-family: "Inter", "Segoe UI", system-ui, sans-serif;
  overflow-x: hidden;
  overflow-y: auto;
  -webkit-font-smoothing: antialiased;
}

.fa-root ::selection {
  background: rgba(239, 68, 68, 0.35);
  color: #fff;
}

/* ---------- ABSTRACT BG (same as landing) ---------- */
.fa-bg {
  position: fixed;
  inset: 0;
  z-index: 0;
  pointer-events: none;
  overflow: hidden;
}

.fa-bg-grain {
  position: absolute;
  inset: -50%;
  width: 200%;
  height: 200%;
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.85' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.04'/%3E%3C/svg%3E");
  opacity: 0.5;
}

.fa-bg-gradient {
  position: absolute;
  inset: 0;
  background:
    radial-gradient(ellipse 80% 60% at 20% 80%, rgba(220,38,38,0.06) 0%, transparent 70%),
    radial-gradient(ellipse 60% 50% at 80% 20%, rgba(249,115,22,0.04) 0%, transparent 70%),
    radial-gradient(ellipse 90% 70% at 50% 50%, rgba(15,15,15,1) 0%, transparent 100%);
}

.fa-bg-line {
  position: absolute;
  background: rgba(255,255,255,0.02);
  width: 1px;
  height: 100%;
  top: 0;
}
.fa-bg-line--1 { left: 20%; }
.fa-bg-line--2 { left: 50%; }
.fa-bg-line--3 { left: 80%; }

.fa-bg-circle {
  position: absolute;
  border-radius: 50%;
  border: 1px solid rgba(255,255,255,0.03);
}
.fa-bg-circle--1 {
  width: 600px; height: 600px;
  top: -180px; right: -120px;
}
.fa-bg-circle--2 {
  width: 400px; height: 400px;
  bottom: -100px; left: -80px;
}

/* ---------- HEADER ---------- */
.fa-header {
  position: sticky;
  top: 0;
  z-index: 20;
  width: 100%;
  border-bottom: 1px solid rgba(255,255,255,0.06);
  background: rgba(10,10,10,0.8);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
}
.fa-header-inner {
  width: 90%;
  max-width: 1400px;
  margin: 0 auto;
  padding: 1.1rem 0;
  display: flex;
  align-items: center;
  justify-content: space-between;
}
.fa-logo {
  display: flex;
  align-items: center;
  gap: 0.7rem;
}
.fa-logo-icon {
  color: #ef4444;
  filter: drop-shadow(0 0 10px rgba(239,68,68,0.5));
}
.fa-logo-text {
  font-family: "Bebas Neue", "Oswald", sans-serif;
  font-size: 1.6rem;
  letter-spacing: 0.06em;
  color: #fff;
}
.fa-logo-accent {
  color: #ef4444;
}
.fa-header-tag {
  font-size: 0.72rem;
  text-transform: uppercase;
  letter-spacing: 0.12em;
  color: rgba(255,255,255,0.25);
  font-weight: 400;
}

/* ---------- MAIN ---------- */
.fa-main {
  position: relative;
  z-index: 1;
  width: 90%;
  max-width: 1400px;
  margin: 0 auto;
  padding: 2.5rem 0 8rem;
}

/* ---------- SHARED CARD ---------- */
.fa-card {
  background: rgba(14,14,14,0.92);
  backdrop-filter: blur(18px);
  -webkit-backdrop-filter: blur(18px);
  border: 1px solid rgba(255,255,255,0.06);
  border-radius: 6px;
  box-shadow: 0 20px 60px rgba(0,0,0,0.35);
  transition: border-color 0.3s ease;
}

/* ---------- BIO SECTION ---------- */
.fa-bio {
  padding: 1.8rem 2.2rem;
  position: relative;
  margin-bottom: 2rem;
  animation: fadeUp 0.6s ease both;
}
.fa-bio-edit-btn {
  position: absolute;
  top: 1.2rem;
  right: 1.4rem;
  background: none;
  border: none;
  color: rgba(255,255,255,0.2);
  cursor: pointer;
  padding: 0.4rem;
  border-radius: 4px;
  transition: color 0.25s, background 0.25s;
}
.fa-bio-edit-btn:hover {
  color: #ef4444;
  background: rgba(239,68,68,0.08);
}
.fa-bio-layout {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 2rem;
  flex-wrap: wrap;
}
.fa-bio-user {
  display: flex;
  align-items: center;
  gap: 1rem;
}
.fa-bio-avatar {
  width: 48px;
  height: 48px;
  border-radius: 50%;
  background: rgba(255,255,255,0.04);
  border: 1px solid rgba(255,255,255,0.08);
  display: flex;
  align-items: center;
  justify-content: center;
  color: rgba(255,255,255,0.3);
  flex-shrink: 0;
}
.fa-bio-tag {
  font-size: 0.68rem;
  text-transform: uppercase;
  letter-spacing: 0.18em;
  color: #ef4444;
  font-weight: 500;
  margin-bottom: 0.2rem;
}
.fa-bio-name {
  font-family: "Oswald", sans-serif;
  font-size: 1.6rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.02em;
  color: #fff;
}

.fa-bio-divider {
  width: 1px;
  height: 40px;
  background: rgba(255,255,255,0.06);
  flex-shrink: 0;
}

.fa-bio-metrics {
  display: flex;
  gap: 2.5rem;
}
.fa-bio-metric {
  text-align: center;
}
.fa-bio-metric-label {
  font-size: 0.65rem;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: rgba(255,255,255,0.3);
  font-weight: 600;
  margin-bottom: 0.3rem;
}
.fa-bio-metric-value {
  font-size: 1.2rem;
  font-weight: 500;
  color: #fff;
}
.fa-bio-metric-unit {
  font-size: 0.7rem;
  color: rgba(255,255,255,0.25);
  font-weight: 400;
}

.fa-bio-computed {
  display: flex;
  gap: 2rem;
}
.fa-bio-computed-label {
  font-size: 0.65rem;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: #ef4444;
  font-weight: 600;
  margin-bottom: 0.3rem;
}
.fa-bio-computed-value {
  font-family: "Oswald", sans-serif;
  font-size: 1.5rem;
  font-weight: 600;
  color: #fff;
}

/* bio inputs (edit mode) */
.fa-bio-input {
  background: rgba(255,255,255,0.04);
  border: 1px solid rgba(255,255,255,0.08);
  border-radius: 4px;
  color: #f0f0f0;
  font-family: "Inter", sans-serif;
  font-size: 0.9rem;
  padding: 0.35rem 0.6rem;
  outline: none;
  transition: border-color 0.25s, box-shadow 0.25s;
}
.fa-bio-input:focus {
  border-color: #ef4444;
  box-shadow: 0 0 0 2px rgba(239,68,68,0.12);
}
.fa-bio-input--name { width: 140px; }
.fa-bio-input--num { width: 60px; text-align: center; }

/* ---------- COLUMNS GRID ---------- */
.fa-columns {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap: 1.4rem;
  margin-bottom: 2.5rem;
  animation: fadeUp 0.6s ease 0.15s both;
}

/* ---------- COLUMN CARD ---------- */
.fa-col {
  display: flex;
  flex-direction: column;
  min-height: 500px;
  overflow: hidden;
}
.fa-col-header {
  padding: 1.2rem 1.4rem;
  border-bottom: 1px solid rgba(255,255,255,0.06);
  display: flex;
  align-items: center;
  justify-content: space-between;
}
.fa-col-title {
  font-family: "Oswald", sans-serif;
  font-size: 0.82rem;
  font-weight: 500;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: #fff;
  display: flex;
  align-items: center;
  gap: 0.5rem;
}
.fa-col-icon--gain { color: #22c55e; }
.fa-col-icon--lost { color: #ef4444; }
.fa-col-icon--other { color: #a855f7; }

/* accent stripe at top of each column */
.fa-col::before {
  content: "";
  display: block;
  height: 2px;
  background: rgba(255,255,255,0.04);
}
.fa-col--gain::before { background: linear-gradient(to right, #22c55e, transparent); }
.fa-col--lost::before { background: linear-gradient(to right, #ef4444, transparent); }
.fa-col--other::before { background: linear-gradient(to right, #a855f7, transparent); }

.fa-col-body {
  padding: 1rem 1.2rem;
  flex: 1;
  overflow-y: auto;
  display: flex;
  flex-direction: column;
  gap: 0.6rem;
}

/* scrollbar styling */
.fa-col-body::-webkit-scrollbar {
  width: 3px;
}
.fa-col-body::-webkit-scrollbar-track {
  background: transparent;
}
.fa-col-body::-webkit-scrollbar-thumb {
  background: rgba(255,255,255,0.06);
  border-radius: 2px;
}

/* ---------- ENTRY ROW ---------- */
.fa-entry {
  display: flex;
  align-items: center;
  gap: 0.7rem;
  padding: 0.7rem 0.85rem;
  background: rgba(255,255,255,0.02);
  border: 1px solid rgba(255,255,255,0.05);
  border-radius: 4px;
  transition: border-color 0.25s, background 0.25s;
}
.fa-entry:hover {
  border-color: rgba(255,255,255,0.1);
  background: rgba(255,255,255,0.035);
}

/* entry select */
.fa-entry-select {
  width: 80px;
  flex-shrink: 0;
  background: transparent;
  border: none;
  font-family: "Inter", sans-serif;
  font-size: 0.68rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.06em;
  color: rgba(255,255,255,0.5);
  cursor: pointer;
  outline: none;
  padding: 0;
  border-right: 1px solid rgba(255,255,255,0.06);
  padding-right: 0.5rem;
  margin-right: 0.2rem;
  appearance: none;
  -webkit-appearance: none;
}
.fa-entry-select option {
  background: #0f0f0f;
  color: #f0f0f0;
}

/* entry input */
.fa-entry-input {
  flex: 1;
  min-width: 0;
  background: transparent;
  border: none;
  font-family: "Inter", sans-serif;
  font-size: 0.85rem;
  color: #f0f0f0;
  outline: none;
}
.fa-entry-input::placeholder {
  color: rgba(255,255,255,0.12);
}
.fa-entry-input--small {
  flex: none;
  width: 50px;
  font-size: 0.75rem;
  text-align: center;
  border-left: 1px solid rgba(255,255,255,0.06);
  padding-left: 0.5rem;
}
.fa-entry-input--cat {
  flex: none;
  width: 80px;
  font-size: 0.68rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.06em;
  color: rgba(255,255,255,0.5);
  border-right: 1px solid rgba(255,255,255,0.06);
  padding-right: 0.5rem;
  margin-right: 0.2rem;
}

/* entry result */
.fa-entry-result {
  min-width: 45px;
  text-align: right;
  font-size: 0.8rem;
  font-weight: 700;
  flex-shrink: 0;
}
.fa-entry-result--gain { color: #22c55e; }
.fa-entry-result--lost { color: #ef4444; }
.fa-entry-result--thinking {
  font-size: 0.68rem;
  font-weight: 400;
  font-style: italic;
  opacity: 0.5;
}
.fa-entry-dot {
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: #a855f7;
  box-shadow: 0 0 8px rgba(168,85,247,0.6);
  flex-shrink: 0;
}

/* ---------- ACTION BUTTON ---------- */
.fa-action-area {
  display: flex;
  justify-content: center;
  padding: 1rem 0 3rem;
  animation: fadeUp 0.6s ease 0.3s both;
}

.fa-summarize-btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 0.6rem;
  font-family: "Inter", sans-serif;
  font-size: 0.85rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  padding: 0.9rem 2.8rem;
  border: none;
  color: #fff;
  background: #ef4444;
  cursor: pointer;
  border-radius: 4px;
  position: relative;
  overflow: hidden;
  transition: transform 0.25s, box-shadow 0.25s;
}
.fa-summarize-btn:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 30px rgba(239,68,68,0.35);
}
.fa-summarize-btn::after {
  content: "";
  position: absolute;
  inset: 0;
  background: linear-gradient(135deg, transparent 40%, rgba(255,255,255,0.12) 50%, transparent 60%);
  transform: translateX(-120%);
  transition: transform 0.5s ease;
}
.fa-summarize-btn:hover::after {
  transform: translateX(120%);
}

.fa-unlock-btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
  font-family: "Inter", sans-serif;
  font-size: 0.8rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  padding: 0.65rem 1.6rem;
  background: transparent;
  color: rgba(255,255,255,0.4);
  border: 1px solid rgba(255,255,255,0.08);
  border-radius: 4px;
  cursor: pointer;
  transition: color 0.25s, border-color 0.25s;
}
.fa-unlock-btn:hover {
  color: #fff;
  border-color: rgba(255,255,255,0.2);
}

/* ---------- ANIMATION ---------- */
@keyframes fadeUp {
  from { opacity: 0; transform: translateY(24px); }
  to { opacity: 1; transform: none; }
}

/* ---------- RESPONSIVE ---------- */
@media (max-width: 900px) {
  .fa-columns {
    grid-template-columns: 1fr;
    gap: 1.2rem;
  }
  .fa-col {
    min-height: 300px;
  }
  .fa-bio-layout {
    flex-direction: column;
    text-align: center;
  }
  .fa-bio-divider {
    width: 60px;
    height: 1px;
  }
  .fa-bio-metrics,
  .fa-bio-computed {
    justify-content: center;
  }
  .fa-bio-user {
    flex-direction: column;
  }
}
